<div class="container">
	<%= simple_form_for [@movie] do |f| %>
			<%= f.input :title %>
			<%= f.input :overview %>
			<%= f.input :rating %>
			<%= f.input :poster_url %>
			<%= f.submit class: 'btn btn-primary' %>
	<% end %>
</div>
